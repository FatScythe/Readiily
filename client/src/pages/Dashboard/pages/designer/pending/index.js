// Hook
import useTitle from "../../../../../hooks/useTitle";

const PendingRequest = () => {
  useTitle("Available Request");
  return <section>AVAILABLE BRAND REQUEST</section>;
};

export default PendingRequest;
