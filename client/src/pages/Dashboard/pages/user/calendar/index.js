// Hook
import useTitle from "../../../../../hooks/useTitle";

const Calendar = () => {
  useTitle("Calendar");
  return (
    <section id='calendar'>
      <h1>Calendar</h1>
    </section>
  );
};

export default Calendar;
